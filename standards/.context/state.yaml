issue_bootstrap:
  updated_at: 2026-04-10T08:45:19.949Z
  latest:
    issue_id: "260"
    issue_title: "fix: stop active-project drift and canonical-main runtime
      persistence pollution"
    issue_body: Harden runtime reliability so active project alignment does not
      drift across issue work and canonical main state surfaces do not get
      dirtied by runtime persistence.
    labels: []
    linked_artifacts: []
    startup_context_paths:
      - /Users/jeking/dev/AgenticOS/projects/agenticos/.project.yaml
      - /Users/jeking/dev/AgenticOS/projects/agenticos/standards/.context/quick-start.md
      - /Users/jeking/dev/AgenticOS/projects/agenticos/standards/.context/state.yaml
      - /Users/jeking/dev/AgenticOS/projects/agenticos/AGENTS.md
      - /Users/jeking/dev/AgenticOS/projects/agenticos/CLAUDE.md
    additional_context: []
    repo_path: /Users/jeking/dev/AgenticOS/worktrees/agenticos-260-stop-active-project-drift-and-main-state-pollution
    project_path: /Users/jeking/dev/AgenticOS/projects/agenticos
    current_branch: fix/260-stop-active-project-drift-and-main-state-pollution
    workspace_type: isolated_worktree
    stages:
      context_reset_performed: true
      project_hot_load_performed: true
      issue_payload_attached: true
    recorded_at: 2026-04-10T08:45:19.949Z
working_memory:
  facts:
    - "GitHub issue #302 was created on 2026-04-19 for the post-0.4.3 runtime
      release parity gap."
    - "Source main already contains merged PR #270 and PR #278, but releases
      still stop at v0.4.3 and the Homebrew formula still points to v0.4.2."
    - Targeted continuity/transcript verification passed locally on 2026-04-18
      in the source tree.
    - "Created GitHub issue #302: Ship release so installed runtime includes
      post-0.4.3 continuity and transcript fixes."
    - Added tasks/issue-302-post-043-runtime-release-parity.md as the local
      execution note for the new issue.
    - "Refreshed standards/.context entry surfaces and CLAUDE.md to point
      project startup context at issue #302."
    - "preflight.ts: Active structural_move enforcement added in
      implementation-affecting task branch — renames detected via git diff
      --name-status --diff-filter=R, blocks if not declared, executes gate
      commands if declared"
    - "preflight.ts: clean_reproducibility_gate is now executed at preflight
      time when structural_move is true and renamed files are present (replaces
      passive storage-only behavior)"
    - "edit-guard.ts: Post-edit scope creep advisory added — after all guard
      checks pass, git diff --name-only is run and undeclared edited files are
      warned via recovery_actions (advisory, not blocking)"
    - "edit-guard.ts: New optional remote_base_branch field added to
      EditGuardArgs interface"
    - npm run build passes with no TypeScript errors
    - "Root audit complete: 15+ items classified across 3 categories"
    - "All overlapping directories compared: mcp-server, scripts, tools,
      AGENTS.md, CLAUDE.md, README.md, CHANGELOG.md, CONTRIBUTING.md,
      ROADMAP.md, LICENSE, .gitignore, .github/ — root is consistently stale vs
      repo"
    - root-git-exit-plan-2026-04-21.md created at standards/knowledge/ with full
      4-phase migration plan
    - Created standards/knowledge/README.md with full index of 96 knowledge
      files grouped by 7 categories, each with one-line description and status
      marker
    - "Added <!-- agenticos-template: v1 --> markers to 8 templates:
      agent-preflight-checklist.yaml, non-code-evaluation-rubric.yaml,
      global-review-log.md, issue-design-brief.md, quick-start.md, state.yaml,
      sub-agent-handoff.md, submission-evidence.md"
    - "#314 fixed: structural_move actively enforced via git diff --name-status
      --diff-filter=R"
    - "#314 fixed: post-edit scope advisory added to edit-guard"
    - "#312 fixed: resolveProjectTarget() canonical function + ~120 lines
      removed from guardrail-evidence"
    - "#312 fixed: version_freshness health gate comparing npm installed vs
      source package.json version"
    - "#313 fixed: 8 template markers (<!-- agenticos-template: v1 -->) added to
      .meta/templates/"
    - "#313 fixed: standards/knowledge/README.md index of 96 files with
      LIVE/SPEC/SUPERSEDED markers"
    - "#311 partially: root-git-exit-plan-2026-04-21.md created, execution
      deferred"
    - "#314 fixed: structural_move actively detected via git diff --name-status
      --diff-filter=R, clean_reproducibility_gate executed when declared, BLOCK
      when not declared but renames detected"
    - "#314 fixed: edit-guard post-edit scope advisory — undeclared files in
      recovery_actions warnings"
    - "#312 fixed: resolveProjectTarget() canonical function in repo-boundary.ts"
    - "#312 fixed: ~120 lines removed from guardrail-evidence.ts via import"
    - "#312 fixed: version_freshness health gate comparing npm runtime vs source
      package.json"
    - "#313 fixed: 8 template markers added to .meta/templates/"
    - "#313 fixed: standards/knowledge/README.md index of 96 files"
    - "#311 partial: root-git-exit-plan-2026-04-21.md created"
  decisions:
    - Treat the next real backlog item as a release/install parity issue, not
      another source-tree continuity redesign.
    - Use the observed quick-start save mismatch as live evidence that the
      active installed runtime is still behind mainline continuity behavior.
    - "Track the next work under GitHub issue #302 and a matching local task
      file."
    - Treat the next active backlog item as release/install parity work rather
      than another source-tree semantics redesign.
    - Use the observed quick-start save mismatch and stale Homebrew formula as
      concrete evidence that the installed runtime remains behind source main.
    - "Start the next implementation flow under issue #302."
    - structural_move detection uses `git diff --name-status --diff-filter=R
      <remote_base> HEAD` filtered to R-prefix lines
    - "When structural_move=false and renames detected: BLOCK with 'Structural
      move detected but not declared'"
    - "When structural_move=true and renames detected: execute
      clean_reproducibility_gate commands, block on any command failure"
    - edit-guard post-edit scope check uses `git diff --name-only <base> HEAD`
      and logs undeclared edits as advisory warnings to recovery_actions only
      (not hard blocks)
    - edit-guard gained optional remote_base_branch parameter to support diff
      comparison
    - "Knowledge directory index: created standards/knowledge/README.md with
      frontmatter (status + date) and grouped tables (Architecture, Guardrail
      Design, Memory Layer, Agent Protocol, Standards, Reviews, Implementation
      Reports)"
    - "Status semantics: LIVE = current guidance, SPEC = design draft under
      consideration, SUPERSEDED = replaced by newer docs"
    - agent-friendly-readme-research marked SUPERSEDED (supplanted by spec),
      open-source-workflow-research marked SUPERSEDED
    - "Template version markers: added <!-- agenticos-template: v1 --> to all 8
      .meta/templates/ files so agenticos_standard_kit_upgrade_check can detect
      content drift"
    - "PR1 #323: preflight structural_move active detection + edit-guard
      advisory scope check"
    - "PR2 #324: unified resolveProjectTarget() + version_freshness health gate"
    - "PR3 #325: 8 template markers + knowledge index README + root-Git exit
      plan doc"
    - "PR4 #311: root-Git plan doc created, actual deletion deferred to Phase 3
      (requires .github/ merge first)"
    - 3 parallel Phase 2 agents covered structural move enforcement, project
      resolution, and knowledge index
    - "#312 agent hit rate limit but all changes were still in the working tree
      when we checked"
    - structural_move preflight enforcement was manually added (agent draft had
      edit-guard changes but skipped preflight.ts)
  pending:
    - "#311 Phase 3: Merge root .github/ and .gitignore into repo, then delete
      stale root duplicates"
    - "#315: Add troubleshooting + consolidate Homebrew caveats"
session:
  last_backup: 2026-04-21T07:12:05.832Z
  id: entry-refresh-2026-04-10
  started: 2026-04-10T10:58:11.098Z
  agent: agenticos-entry-refresh
  last_entry_surface_refresh: 2026-04-19T03:56:50.359Z
current_task:
  title: "Phase 2: PRs #323-325 merged, issues closed"
  status: completed
  updated: 2026-04-21T07:11:57.238Z
  next_step: "Run issue-bootstrap and release-prep implementation flow for #302 in
    an isolated worktree."
loaded_context:
  - .project.yaml
  - standards/.context/quick-start.md
  - tasks/issue-302-post-043-runtime-release-parity.md
  - tasks/issue-266-homebrew-runtime-session-local-release.md
  - tasks/issue-266-pr-draft.md
  - CHANGELOG.md
  - homebrew-tap/Formula/agenticos.rb
entry_surface_refresh:
  refreshed_at: 2026-04-19T03:56:50.359Z
  issue_id: "302"
  summary: "Created issue #302 to track release/install parity after confirming
    that main already contains post-0.4.3 continuity and transcript fixes while
    shipped/runtime distribution remains behind."
  status: active
  current_focus: "Issue #302 is now the active backlog item: ship the next release
    so installed/runtime AgenticOS catches up with post-0.4.3 continuity and
    transcript behavior."
  report_paths:
    - tasks/issue-302-post-043-runtime-release-parity.md
    - tasks/issue-266-homebrew-runtime-session-local-release.md
    - tasks/issue-266-pr-draft.md
  recommended_entry_documents:
    - tasks/issue-302-post-043-runtime-release-parity.md
    - CHANGELOG.md
    - homebrew-tap/Formula/agenticos.rb
